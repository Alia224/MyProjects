name: Cypress Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout your code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup Node.js (change version if needed)
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Install dependencies inside the FDTraining folder
      - name: Install dependencies
        run: |
          cd FDTraining
          npm install

      # Run Cypress tests inside the FDTraining folder
      - name: Run Cypress tests
        run: |
          cd FDTraining
          npx cypress run